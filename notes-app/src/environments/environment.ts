// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  service: "http://local.mylocal.org:4500",
  local: "http://local.mylocal.org:4500"
};

//export const environment = {
//  production: false,
//  clientId: "sample-angular-app",
//  authority: "https://keycloak-ci.aws.labshare.org/auth",
//  service: "https://local.mylocal.org:4200"
//};

// export const environment = {
//   production: false,
//   clientId: "sample-angular-app",
//   authority: "https://a.labshare.org/_api/auth/ncats",
//   service: "https://a-labshare-sample-angular-app.ci.aws.labshare.org"
// };

//export const environment = {
//  production: false,
//  clientId: "sample-angular-app",
//  authority: "https://auth.ncats.nih.gov/_api/v2/auth/ncats",
//  service: "https://una-sample-angular-app.ci.aws.labshare.org"
//};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
